## TargetInstrumentTablesForm モバイル動作チェック

対象: `apps/scout/src/components/TargetInstrumentTablesForm.tsx` のセル入力（モバイルキーボード）

### 事前準備
- 表データを開く
- 表を1つ展開（ヘッダをタップ）
- 対象セルに `0.05` が入っている状態を用意（なければ入力してblurで確定）

### チェック項目
- **小数の途中編集**:
  - `0.05` をタップ → カーソルを末尾付近に置く
  - `6` を入力して `0.056` にできること
  - 途中で `0.0566` のように意図しない重複が起きないこと
  - その後も入力が継続でき、入力不能状態にならないこと
- **置換入力（選択→上書き）**:
  - `6` が入ったセルを全選択（OSの選択UIでも可）→ `2` を入力
  - 結果が `2` になり、`662` のように重複しないこと
- **入力確定タイミング**:
  - 入力中に別セルへ移動（タップ）しても、元セルの値が意図通り確定すること（blur確定）
- **行操作の安全性**:
  - 入力途中で「行を追加」「行を削除」を行っても、表示崩れやクラッシュが起きないこと

